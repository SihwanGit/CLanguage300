#include<stdio.h>

int main() {
	int x, y;
	x = 10;
	y = x - 5;

	if (x > y) printf("x가 y보다 큽니다.\n");
	else printf("y가 x보다 크거나 같습니다.\n");
	//이렇게 중괄호 없이 하나의 Stmt만 오려면 if문의 문법을 if (expr) stmt로 설정해야할 듯.
	//stmt가 한 문장이면 그냥 진행하고, 여러 문장이면 stmts로 재귀 후 복합문을 이어가는 식으로 하면 될 듯.

	return 0;
}